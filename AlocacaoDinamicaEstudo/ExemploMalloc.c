#include <stdio.h>
#include <stdlib.h>

int main(){
    char *p;
    p = (char *) malloc(1000); // espaco para 1000 caracteres
    int *u;
    u = (int *) malloc(1000); // espaco para 250 inteiros
    return 0;
}